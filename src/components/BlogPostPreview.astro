---
import { Post } from '@models/posts'

export interface Props {
  post: Post
}

const { post } = Astro.props
---

<article class="mb-5">
  <header class="flex flex-col gap-3">
    <span>{post.frontmatter.pubDate}</span>
    <a href={post.url} class="p-2 reset-link dark:!text-white text-gray-1000">
      <h2 class="!font-serif">
        {post.frontmatter.title}
      </h2>
    </a>
  </header>
  <p class="!mt-0">{post.frontmatter.description}</p>
  <a href={post.url} class="p-2 !pl-0">Read more</a>
</article>

<style>
  .unset {
    all: unset;
  }
</style>
